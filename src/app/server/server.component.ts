import { Component } from '@angular/core';
// Decorator
// pass a javascript object to configure it
@Component({
  selector: 'app-server',
  templateUrl: './server.component.html'
})

export class ServerComponent {
  
}